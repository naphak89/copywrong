// Temporary test file to verify React is working

export default function TestApp() {
  return (
    <div
      style={{
        padding: "20px",
        backgroundColor: "#f0f0f0",
        minHeight: "100vh",
      }}
    >
      <h1 style={{ color: "#000" }}>React is working!</h1>
      <p>If you see this, React is rendering correctly.</p>
    </div>
  );
}
